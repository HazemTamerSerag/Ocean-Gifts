<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NasaXML" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/stroke</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Background color -->
        <!-- Light background for dark theme -->
        <item name="android:textColor">@color/white</item> <!-- Dark text color -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>  <!-- Dark theme color -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
